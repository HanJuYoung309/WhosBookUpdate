<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.example.whosbookupdate.mapper.MemberMapper">

    <resultMap id="MemberResultMap" type="com.example.whosbookupdate.domain.MemberVO">
        <id property="memberId" column="member_id"/> <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="nickname" column="nickname"/>
    </resultMap>

    <insert id="insertMember" parameterType="com.example.whosbookupdate.domain.MemberVO">
        INSERT INTO MEMBER
        (email,nickname,password,status,introduction)
        values
            (#{email},#{nickname},#{password},#{status},#{introduction})
    </insert>


    <select id="findByMemberId" resultType="com.example.whosbookupdate.domain.MemberVO">
        SELECT * FROM MEMBER WHERE MEMBER_ID=#{memberId}
    </select>

    <select id="findByUserName" resultType="com.example.whosbookupdate.domain.MemberVO">
        SELECT * FROM member where nickname=#{nickname}
    </select>


    <select id="findByEmail" resultMap="MemberResultMap">
        SELECT * FROM member WHERE email = #{email}
    </select>
</mapper>
